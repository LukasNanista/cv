WpfMiniProject

WPF app with two linked forms.
First takes in a person's info and another that takes in address info (multiple per person).
Person form has a list box and a button to open other form where new address is created and added to the first form.
